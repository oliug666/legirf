<UserControl x:Class="TPIH.Gecco.WPF.Views.StatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:TPIH.Gecco.WPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <viewModels:StatusViewModel/>
    </UserControl.DataContext>
    <Grid>
        <ListView ItemsSource="{Binding Commands}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Command" Width="100" DisplayMemberBinding="{Binding Info}"/>
                    <GridViewColumn Header="Message" Width="100"  DisplayMemberBinding="{Binding Message}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
